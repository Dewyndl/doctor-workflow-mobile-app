import { StyleSheet } from "react-native";

export const injectionZonesSelectStyles = StyleSheet.create({
    container: {
        paddingHorizontal: 16,
        alignItems: 'center',
        width: '100%',
    },
    titleContainer: {
        marginBottom: 30,
        gap: 10,
    },
    injectionZonesContainer: {
        gap: 10,
        marginBottom: 20,
    },
    buttonsContainer: {
        width: '100%',
    },
})