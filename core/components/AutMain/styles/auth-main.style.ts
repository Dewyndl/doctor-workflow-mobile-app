import { StyleSheet } from "react-native";

export const authMainStyles = StyleSheet.create({
    container: {
        paddingHorizontal: 25,
        paddingTop: 60
    },
    imageContainer: {
        width: '100%',
        marginBottom: 124
    },
    buttonContainer: {
        gap: 10
    },
    bottomContainer: {
        paddingTop: 60
    }
})