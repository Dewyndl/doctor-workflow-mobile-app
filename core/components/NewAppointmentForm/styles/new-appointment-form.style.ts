import { StyleSheet } from "react-native";

export const newAppointmentFormStyles = StyleSheet.create({
    container: {
        paddingHorizontal: 16,
        alignItems: 'center',
        paddingBottom: 60,
    },
    titleContainer: {
        marginBottom: 30,
        gap: 10,
        maxWidth: 210
    },
    title: {
        marginVertical: 12,
        maxWidth: 270
    },
    inputsContainer: {
        gap: 10,
    }
})