import { StyleSheet } from "react-native";

export const headerStyle = StyleSheet.create({
  container: {
    padding: 20,
  },
  titleBlock: {
    flex: 1,
    alignItems: 'center',
    justifyContent: 'center',
  },
  emptyView: {
    width: 20,
  },
  rightElement: {
    minWidth: 20,
  },
});