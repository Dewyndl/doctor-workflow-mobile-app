import { StyleSheet } from 'react-native';

export const appointmentCalendarPageStyles = StyleSheet.create({
  container: {
    paddingHorizontal: 16,
  },
  bottomContainer: {
    paddingTop: 40,
  },

  usersContainer: {
    paddingTop: 30,
    paddingBottom: 30,
    gap: 10,
  },
  seminar: {
    paddingVertical: 8,
    paddingHorizontal: 16,
    borderRadius: 8,
    backgroundColor: '#FFF'
  }
});
