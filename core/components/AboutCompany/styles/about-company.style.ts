import { StyleSheet } from 'react-native';

export const aboutCompanyStyles = StyleSheet.create({
  scrollContent: {
    paddingHorizontal: 16,
    paddingBottom: 24,
  },
  body: {
    textAlign: 'justify',
  },
});
