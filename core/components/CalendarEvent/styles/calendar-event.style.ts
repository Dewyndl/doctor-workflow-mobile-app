import { StyleSheet } from "react-native";

export const calendarEventStyles = StyleSheet.create({
    container: {
       gap: 360,
       paddingHorizontal: 16,
    },
    buttonsContainer: {
        width: '100%',
        paddingHorizontal: 34,
        gap: 10
    }
});